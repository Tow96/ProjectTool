import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ProjectsRouting } from './projects.routing';

@NgModule({
  imports: [CommonModule, ProjectsRouting],
})
export class ProjectsModule {}
